// object
let user: {
    id?: number;
    name: string;
} = {
    id: 1,
    name: '이수빈',
}

let config: {
    readonly apiKey: string,
} = {
    apiKey: "My API KEY",
};

// config.apiKey = 'hacked';